// Optional helper file for authentication
module.exports = {
  requireLogin: (req, res, next) => {
    if (!req.session.userId) return res.status(401).send('Unauthorized');
    next();
  }
};
